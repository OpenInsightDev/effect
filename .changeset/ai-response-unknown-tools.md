---
"effect": minor
---

Add `Response.AllPartsView`, `Response.PartView`, and `Response.StreamPartView` schemas, along with `Response.ToolCallPartsView` and `Response.ToolResultPartsView` types, for responses that may contain tools outside the provided toolkit.
